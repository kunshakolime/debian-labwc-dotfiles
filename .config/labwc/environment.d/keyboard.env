# Alternative way to set keyboard layout (files in environment.d/ are merged)
# This is here to show the pattern — the main environment file already sets it.
# You can add variant or model here too:
# XKB_DEFAULT_VARIANT=
# XKB_DEFAULT_MODEL=pc105
