<?xml version="1.0" encoding="UTF-8"?>
<labwc_config>
  <desktops number="9" />
  <core>
    <gap>6</gap>
  </core>

  <!-- Keyboard bindings -->
  <keyboard>
    <default />

    <!-- Launcher -->
    <keybind key="W-Space">
      <action name="Execute" command="menu fuzzel" />
    </keybind>

    <!-- Terminal -->
    <keybind key="W-Return">
      <action name="Execute" command="footclient" />
    </keybind>


    <!-- Close window -->
    <keybind key="W-q">
      <action name="Close" />
    </keybind>

    <!-- Control center (clipboard, wallpaper, layout, power) -->
    <keybind key="W-p">
      <action name="Execute" command="menu control" />
    </keybind>

    <!-- Toggle the whole waybar (reclaim screen real estate) -->
    <keybind key="W-b">
      <action name="Execute" command="bar" />
    </keybind>

    <!-- Window cycling -->
    <keybind key="W-Tab">
      <action name="NextWindow" />
    </keybind>
    <keybind key="W-S-Tab">
      <action name="PreviousWindow" />
    </keybind>

    <!-- Workspaces: switch (W-N) and send window (W-S-N) -->
    <keybind key="W-1"><action name="GoToDesktop" to="1" /></keybind>
    <keybind key="W-2"><action name="GoToDesktop" to="2" /></keybind>
    <keybind key="W-3"><action name="GoToDesktop" to="3" /></keybind>
    <keybind key="W-4"><action name="GoToDesktop" to="4" /></keybind>
    <keybind key="W-5"><action name="GoToDesktop" to="5" /></keybind>
    <keybind key="W-6"><action name="GoToDesktop" to="6" /></keybind>
    <keybind key="W-7"><action name="GoToDesktop" to="7" /></keybind>
    <keybind key="W-8"><action name="GoToDesktop" to="8" /></keybind>
    <keybind key="W-9"><action name="GoToDesktop" to="9" /></keybind>
    <keybind key="W-S-1"><action name="SendToDesktop" to="1" follow="no" /></keybind>
    <keybind key="W-S-2"><action name="SendToDesktop" to="2" follow="no" /></keybind>
    <keybind key="W-S-3"><action name="SendToDesktop" to="3" follow="no" /></keybind>
    <keybind key="W-S-4"><action name="SendToDesktop" to="4" follow="no" /></keybind>
    <keybind key="W-S-5"><action name="SendToDesktop" to="5" follow="no" /></keybind>
    <keybind key="W-S-6"><action name="SendToDesktop" to="6" follow="no" /></keybind>
    <keybind key="W-S-7"><action name="SendToDesktop" to="7" follow="no" /></keybind>
    <keybind key="W-S-8"><action name="SendToDesktop" to="8" follow="no" /></keybind>
    <keybind key="W-S-9"><action name="SendToDesktop" to="9" follow="no" /></keybind>

    <!-- Screenshots -->
    <keybind key="W-Print">
      <action name="Execute" command="grim" />
    </keybind>
    <keybind key="W-S-Print">
      <action name="Execute" command='grim -g "$(slurp)" - | swappy -f -' />
    </keybind>

    <!-- Volume control -->
    <keybind key="XF86AudioRaiseVolume">
      <action name="Execute" command="volume up" />
    </keybind>
    <keybind key="XF86AudioLowerVolume">
      <action name="Execute" command="volume down" />
    </keybind>
    <keybind key="XF86AudioMute">
      <action name="Execute" command="volume mute" />
    </keybind>

    <!-- Brightness (2% steps) -->
    <keybind key="XF86MonBrightnessUp">
      <action name="Execute" command="brightness up" />
    </keybind>
    <keybind key="XF86MonBrightnessDown">
      <action name="Execute" command="brightness down" />
    </keybind>

    <!-- Night light warmth (Super + F9/F10) -->
    <keybind key="W-F9">
      <action name="Execute" command="nightlight warmer" />
    </keybind>
    <keybind key="W-F10">
      <action name="Execute" command="nightlight cooler" />
    </keybind>

    <!-- Media controls -->
    <keybind key="XF86AudioPlay">
      <action name="Execute" command="playerctl play-pause" />
    </keybind>
    <keybind key="XF86AudioNext">
      <action name="Execute" command="playerctl next" />
    </keybind>
    <keybind key="XF86AudioPrev">
      <action name="Execute" command="playerctl previous" />
    </keybind>
  </keyboard>

  <!-- Window snapping/maximizing behavior -->
  <windowSnapMaxRange>10</windowSnapMaxRange>

  <!-- Theme -->
  <theme>
    <name>Default</name>
    <cornerRadius>6</cornerRadius>
  </theme>
</labwc_config>
