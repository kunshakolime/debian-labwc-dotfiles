#!/bin/bash
# Toggle a fuzzel-based command: close any open fuzzel menu, otherwise run it.
# Usage: menu <command...>   e.g. menu fuzzel   /   menu control

if pgrep -x fuzzel >/dev/null 2>&1; then
    pkill -x fuzzel
    exit 0
fi

exec "$@"
