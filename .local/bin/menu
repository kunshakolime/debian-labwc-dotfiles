#!/bin/bash
# Toggle a wofi-based command: close any open wofi menu, otherwise run it.
# Usage: menu <command...>   e.g. menu wofi   /   menu control

if pgrep -x wofi >/dev/null 2>&1; then
    pkill -x wofi
    exit 0
fi

exec "$@"
